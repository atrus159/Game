with(text_element){
	instance_destroy(self);
}
create_header("He looks over the papers. Then he looks them over again, scowling all the while.\n"+control.tomas_name+" - \"Fine. Go.\"");
create_space(level_2_bar_scrpt)