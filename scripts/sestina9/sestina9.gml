with(text_element){
	instance_destroy(self);
}
create_header("and the child draws another inscrutable house.");
create_space(exit_dialogue);
camera_set_view_size(mouse.myCamera,640,480);
camera_set_view_pos(mouse.myCamera,interactable.x-320,interactable.y-240);
