with(text_element){
	instance_destroy(self);
}
benny_char.direction = 180+90;
camera_set_view_size(mouse.myCamera,640,480);
create_header("He goes back to staring blankly across the street, sweating a little.");
create_space(benny_exit);